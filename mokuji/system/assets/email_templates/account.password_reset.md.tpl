# Password reset for {{site_name}}.

You requested a password reset for e-mail address {{user.email}}.

Open the following link in your web-browser to set a new password:
[{{claim_url}}]({{claim_url}})

The password reset link will expire at {{token_expires}}.
If you didn't request a password reset you can safely ignore this message.